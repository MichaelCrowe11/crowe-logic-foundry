# Extra dependencies required only by the FastAPI control plane.
# Installed on top of requirements.txt by Dockerfile.control-plane.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
asyncpg>=0.30.0
bcrypt>=4.2.0
PyJWT>=2.9.0
pydantic>=2.9.0
stripe>=10.0.0
